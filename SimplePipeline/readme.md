# Simple Pipeline 
This is the CPU with just the pipelined stages (no hazard unit). It follows this image:
[Simple Pipeline Reference Diagram](./../images/PipelineImageSimp.png)

## Testing
Similar to the main CPU, go to the `tb` folder and type the command:
```
chmod +x doit.sh
./doit.sh
```

You'll see something like this:
[Expected Results for Simple Pipeline Tests](./../images/SimpPipelineResult.png)
